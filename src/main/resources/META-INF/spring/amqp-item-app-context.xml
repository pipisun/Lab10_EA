<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:rabbit="http://www.springframework.org/schema/rabbit"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/rabbit
        http://www.springframework.org/schema/rabbit/spring-rabbit.xsd">

   <rabbit:connection-factory id="amqpConnectionFactory" host="localhost" username="joe" password="joe"/>
 
    <rabbit:template id="amqpTemplate" connection-factory="amqpConnectionFactory"
                     reply-timeout="2000" routing-key="purchases.phone"
                     exchange="item" />
                     
	<rabbit:admin connection-factory="ampqConnectionFactory" />

	<rabbit:queue name="inQueue" />
	<rabbit:queue name="outQueue" />

	<rabbit:direct-exchange name="item">
		<rabbit:bindings>
			<rabbit:binding queue="inQueue" key="in.key" />
			<rabbit:binding queue="outQueue" key="out.key" />
		</rabbit:bindings>
	</rabbit:direct-exchange>
	
 </beans>
